#include <stdio.h>
#include <stdbool.h>

double findMedianSortedArrays(int* nums1, int nums1Size, int* nums2, int nums2Size){
    // Binary search - ish
    /**
     * We want to find two "cuts", one in each array, such that: 
     * 1.   the total number of elements in the two left side partitions is the same as the total number of elements in the two
     *      right side partitions.
     * 2.   any elements in any left side partition is smaller than any element in the right side partitions.
     * */


    

    return 0;
}
